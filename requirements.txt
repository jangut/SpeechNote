# ── SpeechNote 依赖 ─────────────────────────────────────
# pip install -r requirements.txt

# === GUI ===
PySide6>=6.11

# === 音频 ===
sounddevice>=0.5
numpy>=2.0

# === ASR（二选一）===
# 方案 A（推荐，省 4GB）：方案 B 跑通后可卸载 PyTorch
funasr_onnx>=1.0
onnxruntime>=1.20

# 方案 B（PyTorch，备选）：若方案 A 无法使用
# funasr>=1.3
# torch>=2.0  # --index-url https://download.pytorch.org/whl/cpu
# modelscope>=1.30

# === LLM 纠错 ===
httpx>=0.28
PyYAML>=6.0

# === 文本纠错 ===
pypinyin>=0.55

# === 打包工具 ===
# pip install pyinstaller>=7.0
# pyinstaller --onedir main.py
